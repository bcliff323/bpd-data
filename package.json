{
	"name": "bpd-data",
	"version": "1.0.0",
	"description": "1. Download [Docker Desktop](https://www.docker.com/products/docker-desktop/)",
	"license": "ISC",
	"author": "",
	"type": "module",
	"main": "",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"sequelize-auto": "sequelize-auto --env-file=.env -o './db/models' -d copdb -h localhost -u user -p 5432 -x password -e postgres",
		"seed": "node --env-file=.env ./seed.js"
	},
	"dependencies": {
		"@sequelize/postgres": "^7.0.0-alpha.46",
		"pg": "^8.16.3",
		"pg-hstore": "^2.3.4"
	},
	"devDependencies": {
		"csv-parser": "^3.2.0",
		"sequelize-auto": "^0.8.8",
		"sequelize-cli": "^6.6.3"
	}
}
